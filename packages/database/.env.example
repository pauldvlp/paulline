# Prisma CLI reads this file. The relative SQLite path resolves against the
# schema directory (packages/database/prisma), so it climbs to the repo-root
# ./data dir. The api runtime uses the root-level DATABASE_URL instead.
DATABASE_URL=file:../../../data/paulline.db
