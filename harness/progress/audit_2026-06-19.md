# AUDIT — Paulline (2026-06-19)

**Estándar:** conventions.md + architecture.md + checkpoints.md + stack.pins  
**Alcance:** Código generado en SETUP-001 (apps/, packages/)  
**Ejecutado:** `/paulness audit` (read-only)

---

## Categoría: Estructura / Layout

| Severidad | Hallazgo | Ubicación | Detalle |
|-----------|----------|-----------|---------|
| ⚠️ importante | Módulo health fuera de modules/ | `apps/api/src/health/` | Según architecture.md, todos los módulos deben estar bajo `modules/`. El módulo `health/` está al nivel de `src/` en lugar de `src/modules/health/`. |

**Regla violada:** architecture.md § "Backend — Hexagonal por dominio" — "Cada módulo (`auth`, `machines`, `tunnels`, `monitoring`)"

---

## Categoría: Arquitectura Consistente

| Severidad | Hallazgo | Ubicación | Detalle |
|-----------|----------|-----------|---------|
| ⚠️ importante | Health no sigue Hexagonal | `apps/api/src/health/*.ts` | Módulo `health` contiene solo `controller` + `service` + `module.ts`. Falta la estructura Hexagonal: no hay `domain/`, `application/`, `infrastructure/`. Resto de módulos (`auth`, `machines`, `tunnels`, `monitoring`) están vacíos (`.gitkeep`), por lo que aún no pueden auditarse. |

**Regla violada:** conventions.md § "Backend — Hexagonal Architecture" + architecture.md § "Backend — Hexagonal por dominio"

---

## Categoría: Datos

| Severidad | Hallazgo | Ubicación | Detalle |
|-----------|----------|-----------|---------|
| ✅ OK | Schema.prisma PK correcto | `packages/database/prisma/schema.prisma` | Todos los modelos (User, Machine, Tunnel, UptimeRecord) usan `cuid()` como PK. ✅ Correcto (no `text` genérico). |

---

## Categoría: Versiones / Pins

| Severidad | Hallazgo | Ubicación | Detalle |
|-----------|----------|-----------|---------|
| ✅ OK | Stack pins aplicados | `apps/api/`, `apps/web/`, `packages/*/` | Node.js LTS, pnpm, NestJS, React, Prisma, Zod: versiones en `package.json` coinciden con stack declarado en config.jsonc. No hay pins específicos de versión major en config, por lo que se usa latest stable (OK). |

---

## Categoría: Reglas Núcleo

| Severidad | Hallazgo | Ubicación | Detalle |
|-----------|----------|-----------|---------|
| ✅ OK | Sin magic values de negocio | Repo entero | No se detectaron strings/numbers hardcodeados sin constantizar. (`MACHINES_RESOURCE_PATH`, `TUNNELS_RESOURCE_PATH`, `HEALTH_STATUS_OK` están constantizados.) |
| ✅ OK | Env validado por Zod | `apps/api/src/config/env.ts` | Todo acceso a environment usa `apiEnvSchema.safeParse(process.env)` + `env.*`. Ningún acceso directo a `process.env` sin validación. |
| ⚠️ menor | Const sin `as const` | `packages/sdk/src/PaulineClient.ts:5-6` | `MACHINES_RESOURCE_PATH` y `TUNNELS_RESOURCE_PATH` son `const` pero falta `as const`. Debería ser: `const MACHINES_RESOURCE_PATH = 'machines' as const;` para que el tipo sea literal (`'machines'` en lugar de `string`). |
| ✅ OK | Sin enum de TS | Repo entero | No hay `enum` de TypeScript. Se usa `as const` (NODE_ENVIRONMENTS, HEALTH_STATUS). ✅ Correcto. |
| ✅ OK | Sin Co-Authored-By | Repo entero (aún no hay commits) | No hay comentarios que revelen autoría de IA. ✅ OK. |
| ✅ OK | Frontera SDK respetada | `apps/web/src/App.tsx` | Código de frontend no hace fetch/HTTP directo; la arquitectura permite import de SDK (aún no usado en App.tsx, pero infraestructura está lista). |
| ✅ OK | Un artefacto por archivo (verificación spot) | `apps/api/src/health/`, `packages/sdk/src/` | Los archivos visitados contienen un concepto principal cada uno (health.controller = HealthController, health.service = HealthService, PaulineClient = clase principal, ResourceClient = clase auxiliar). ✅ OK. |

---

## Categoría: Componentes / Estilos (Frontend)

| Severidad | Hallazgo | Ubicación | Detalle |
|-----------|----------|-----------|---------|
| ❌ bloqueante | shadcn/ui NO instalado | `apps/web/package.json` | Config.jsonc declara `componentLibrary: "shadcn/ui"`, pero la dependency NO está en `package.json`. Convention.md § "Componentes & shadcn/ui" — "Usar shadcn siempre". Falta: `@radix-ui/*`, `react-hook-form`, `@hookform/resolvers`, `class-variance-authority`, `clsx` y el componente registry de shadcn. |
| ✅ OK | Tailwind instalado | `apps/web/package.json:27` | Tailwind v3.4.17 presente. ✅ Correcto. |
| ✅ OK | Atomic Design estructura | `apps/web/src/components/` | Carpetas presentes: atoms/, molecules/, organisms/, templates/, pages/. ✅ Correcto. |
| ⚠️ importante | React Hook Form NO instalado | `apps/web/package.json` | Convention.md § "Forms: React Hook Form + Zod Pattern" especifica que todo formulario debe usar react-hook-form + zodResolver. Paquetes faltantes: `react-hook-form`, `@hookform/resolvers`. |

---

## Categoría: Tests

| Severidad | Hallazgo | Ubicación | Detalle |
|-----------|----------|-----------|---------|
| ⚠️ importante | Tests pendientes para módulos vacíos | `apps/api/src/modules/{auth,machines,tunnels,monitoring}/` | Los módulos principales están vacíos (solo `.gitkeep`). No hay criterios de aceptación definidos para auditar tests. Desbloqueador: implementación de AUTH-001 en próxima feature. |
| ✅ OK | Tests presentes para core | Repo entero | SETUP-001 incluye 4 test files: health.service.test.ts, api-env.test.ts, PaulineClient.test.ts, App.test.tsx. 13 tests total verdes (verificado en progress/current.md). ✅ Correcto. |
| ⚠️ importante | E2E no presente | `apps/` | Convention.md § "Tests" — "E2E: Playwright (para flujos críticos)". No hay tests E2E configurados ni archivo base de Playwright. Próxima feature (AUTH-001) debería incluir E2E para login. |

---

## Categoría: Convenciones no compuestas

| Severidad | Hallazgo | Ubicación | Detalle |
|-----------|----------|-----------|---------|
| ⚠️ importante | React Hook Form + Zod no documentado en spec | `harness/docs/conventions.md` | El convention pack ya está documentado (lo vimos en conventions.md), pero **el SDK correspondiente (react-hook-form, @hookform/resolvers) NO está instalado**. Esto es una gap entre spec (que menciona React Hook Form) e implementación (que le falta el package). No es un gap de composición del harness, sino una dependencia faltante del proyecto. (Moviéndolo a categoría de artefactos faltantes.) |

---

## Resumen por Severidad

| Severidad | Cantidad | Detalles |
|-----------|----------|----------|
| ❌ **Bloqueantes** | 1 | shadcn/ui NO instalado (violación core de convention) |
| ⚠️ **Importantes** | 6 | Health no en modules/; Health no Hexagonal; React Hook Form missing; E2E missing; módulos vacíos; const sin `as const` en SDK |
| ✅ **Alineado** | 8+ | PK tipo correcto, env validado, no enums, no Co-Authored-By, SDK boundary respetada, Tailwind presente, Atomic Design estructura, tests core OK |

**Total: 1 bloqueante, 6 importantes, 0 menores · 0 gaps de composición.**

---

## Acciones Recomendadas

### **Opción A: Minimal (permite ship SETUP-001)**
1. Ignorar hallazgos por ahora; son correcciones que aplican a **features futuras** (AUTH-001, components en DASHBOARD-001, E2E en MACHINES-001).
2. SETUP-001 cumple su spec: ✅ monorepo, ✅ docker, ✅ health check, ✅ dev setup.
3. Remediar en el backlog dedicado (INFRA-001 o similar) tras ship.

**Justificación:** SETUP-001 es scaffolding mínimo. shadcn/ui, React Hook Form, E2E, Hexagonal en health — se aplican cuando implementemos features funcionales (AUTH, MACHINES, TUNNELS). No bloquean el MVP.

### **Opción B: Strict (antes de ship)**
1. Crear feature `INFRA-001: Fix architecture violations (health → modules/, shadcn+RHF setup, E2E scaffold)`.
2. Remediar los 7 hallazgos antes de hacer commit de SETUP-001.
3. Retardar ship, pero garantiza que el harness está alineado desde el inicio.

**Justificación:** Evita acumular tech debt. Las convenciones existen para eso.

---

## Reporte: Read-only (no cambié nada)

Para **materializar remedios** (Opción A o B), ejecuta:
- **`/paulness audit --fix`** — propone features de remediación en el backlog
- **`/paulness next`** — conduce cada feature por SDD (spec → implementación → review)

**¿Cuál prefieres? Opción A (ship SETUP-001, remediar luego) u Opción B (remediar antes de ship)?**
